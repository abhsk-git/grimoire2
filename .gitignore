# Python
api/venv/
api/__pycache__/
api/blueprints/__pycache__/
api/*.pyc

# Secrets
api/config/.env

# Next.js
frontend/.next/
frontend/node_modules/

# Audit reports
SECURITY_AUDIT.md

# Uploads (symlinked, not tracked)
static/
api/static/uploads/

# …but DO track the bundled weeb sticker pack (source assets)
!api/static/
api/static/*
!api/static/stickers/
!api/static/stickers/**

# DB backups (contain comment data — never commit)
api/backups/

# One-off local scripts
restore_posts.py
